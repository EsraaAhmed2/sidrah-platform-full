// Auto-collected i18n dictionaries per area.
// Each file exports: export const dict = { ar: {...}, en: {...} }
import { dict as auth } from "./auth";
import { dict as learning } from "./learning";
import { dict as userPages } from "./user-pages";
import { dict as admin } from "./admin";

export const areaDicts = [auth, learning, userPages, admin];
